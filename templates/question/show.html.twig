{% extends 'base.html.twig' %}

{% block title %}{{ question.title }}{% endblock %}

{% block body %}

    <h1 class="mb-20">{{ question.title }}</h1>
    <p>{{ question.content }}</p>
{% endblock %}
