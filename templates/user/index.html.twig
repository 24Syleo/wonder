{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
    <div class="block">
        <h2 class="pb-30">Page de profil</h2>
        {{  form_start(form) }}
        {{  form_rest(form) }}
        <button class="mr-10 btn btn-primary">
            Inscription
        </button>
        <small>
            <a href="{{ path('login') }}">Vous avez déjà un compte ?</a>
        </small>
        {{  form_end(form) }}
    </div>
{% endblock %}
